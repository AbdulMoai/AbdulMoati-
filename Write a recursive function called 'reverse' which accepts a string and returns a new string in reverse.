function reverse(str, len) {
            if (len === str.length) {
                return;
            }
            reverse(str, len + 1);
 
            document.write(str[len]);
 
        }
 
        let a = "why all programers use Hello world";
 
        reverse(a, 0);
